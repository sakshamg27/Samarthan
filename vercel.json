{
  "version": 2,
  "buildCommand": "curl -fsSL https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.24.0-stable.tar.xz | tar -xJ -C /tmp && export PATH=\"/tmp/flutter/bin:$PATH\" && cd frontend && flutter pub get && flutter build web --release",
  "outputDirectory": "frontend/build/web",
  "installCommand": "echo 'Installing Flutter during build...'"
}
